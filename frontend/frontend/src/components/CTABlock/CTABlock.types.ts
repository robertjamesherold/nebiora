import type { ButtonsProps } from '@/ui/Buttons/Buttons.types';

type CTABlockProps = {
  heading: string;
  description?: string;
  buttons: ButtonsProps[];
  className?: string;
};

export type { CTABlockProps };
