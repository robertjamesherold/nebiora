import type { ReferenzenProps } from './Referenzen.types';

const ReferenzenData: ReferenzenProps = {
  category: 'Referenzen',
  heading: ['Marken, die wir', 'mitgeprägt haben.'],
  description: 'Projekte, in denen Markenidentität und Auftritt aus einem Guss entstanden sind.',
};

export default ReferenzenData;
