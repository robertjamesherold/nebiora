import type { ReferenzenProps } from './Referenzen.types';

const ReferenzenData: ReferenzenProps = {
  category: 'Referenzen',
  heading: ['Technik, die', 'zuverlässig läuft.'],
  description: 'Projekte, in denen solide Entwicklung den Unterschied gemacht hat.',
};

export default ReferenzenData;
