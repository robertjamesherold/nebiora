import type { ReferenzenProps } from './Referenzen.types';

const ReferenzenData: ReferenzenProps = {
  category: 'Referenzen',
  heading: ['Webdesign, das', 'überzeugt.'],
  description: 'Ein Auszug aus Projekten, in denen wir Design und Wirkung zusammengebracht haben.',
};

export default ReferenzenData;
