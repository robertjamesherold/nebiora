import type { ProjectGridProps } from './ProjectGrid.types';

const ProjectGridData: ProjectGridProps = {
  category: 'Projekte',
  heading: 'Alle Arbeiten im Überblick',
  description:
    'Jedes Projekt hat eigene Ziele, eigene Herausforderungen und eine eigene Lösung — ein Blick in unsere vollständige Portfolio-Arbeit.',
};

export default ProjectGridData;
