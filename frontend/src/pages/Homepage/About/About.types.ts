type AboutProps = {
  category: string;
  heading: string[];
  description: string;
  buttonText: string;
  buttonLink: string;
  cards: {
    title: string;
    description: string;
  }[];
};

export type { AboutProps };
