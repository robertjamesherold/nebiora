import type { ReferenzenProps } from './Referenzen.types';

const ReferenzenData: ReferenzenProps = {
  category: 'Referenzen',
  heading: ['Shops, die', 'überzeugen.'],
  description: 'Projekte, in denen wir Produktseiten, Checkout und Zahlungsanbindung zusammen gedacht haben.',
};

export default ReferenzenData;
