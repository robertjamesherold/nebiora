import type { ReferenzenProps } from './Referenzen.types';

const ReferenzenData: ReferenzenProps = {
  category: 'Referenzen',
  heading: ['Sichtbarkeit, die sich', 'auszahlt.'],
  description: 'Projekte, in denen Struktur und Inhalt gemeinsam für Sichtbarkeit gesorgt haben.',
};

export default ReferenzenData;
